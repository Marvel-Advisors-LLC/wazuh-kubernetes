#!/bin/bash


# Go to the scripts directory (relative to the repo root)
cd "$(git rev-parse --show-toplevel)/wazuh/sops-scripts"

# Execute verify script
./verify-secrets-dif.sh

# Capture the result
if [ $? -ne 0 ]; then
  echo "❌ Secret verification failed! Commit aborted."
  exit 1
fi
